{
  "name": "panabarbero",
  "version": "4.2.6",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "ultracite lint",
    "format": "ultracite format",
    "test": "turbo test",
    "analyze": "turbo analyze",
    "translate": "turbo translate",
    "boundaries": "turbo boundaries",
    "bump-deps": "bunx npm-check-updates --deep -u -x react-day-picker && pnpm install",
    "bump-ui": "bunx shadcn@latest add --all --overwrite -c packages/design-system",
    "migrate": "cd packages/database && bunx prisma format && bunx prisma generate && bunx prisma db push",
    "clean": "git clean -xdf node_modules"
  },
  "devDependencies": {
    "@auto-it/first-time-contributor": "^11.3.0",
    "@biomejs/biome": "1.9.4",
    "@pbarb/typescript-config": "*",
    "@turbo/gen": "^2.5.0",
    "@types/bun": "^1.2.9",
    "tsup": "^8.4.0",
    "turbo": "^2.5.0",
    "typescript": "^5.8.3",
    "ultracite": "^4.2.1"
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "bun@1.2.9",
  "dependencies": {
    "@clack/prompts": "^0.10.1",
    "commander": "^13.1.0"
  },
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "trustedDependencies": [
    "@biomejs/biome",
    "@clerk/shared",
    "@sentry/cli",
    "core-js",
    "core-js-pure"
  ]
}
